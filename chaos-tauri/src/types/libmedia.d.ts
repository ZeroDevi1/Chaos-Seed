declare module '@libmedia/avutil/dist/esm/enum.js' {
  // Runtime enum object used by libmedia's ESM build.
  // Keep it loosely typed; we only need a handful of numeric constants.
  export const AVCodecID: Record<string, number>
}

