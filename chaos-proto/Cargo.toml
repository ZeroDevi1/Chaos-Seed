[package]
name = "chaos-proto"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "IPC protocol DTOs for chaos-seed (NamedPipe JSON-RPC)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

