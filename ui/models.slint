export struct SubtitleRow {
    score: string,
    name: string,
    ext: string,
    languages: string,
    extra_name: string,
}

export struct DanmakuRow {
    id: int,
    user: string,
    text: string,
    start_ms: int,
    end_ms: int,
    lane: int,
    width_est: length,
}
