export struct SubtitleRow {
    score: string,
    name: string,
    ext: string,
    languages: string,
    extra_name: string,
}

export struct DanmakuRow {
    id: int,
    user: string,
    text: string,
    image_url: string,
    image_w: length,
    image: image,
    image_ready: bool,
    start_ms: int,
    end_ms: int,
    lane: int,
    width_est: length,
}
